import 'package:flutter/material.dart';

class detailTT extends StatelessWidget {
  const detailTT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
